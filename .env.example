# =============================================================================
# AudioNoise Web - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# SECURITY: Never commit .env to version control!
#
# Legend:
#   [REQUIRED]  - Must be set for the application to function
#   [REQUIRED*] - Required in production only
#   [OPTIONAL]  - Has sensible defaults or enables optional features
# =============================================================================

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# [OPTIONAL] Node environment: development | production | test
# Default: development
NODE_ENV=development

# [OPTIONAL] Server port
# Default: 5000 (Note: Other ports may be firewalled in some environments)
PORT=5000

# =============================================================================
# DATABASE
# =============================================================================

# [REQUIRED] PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# Example: postgresql://audionoise:password@localhost:5432/audionoise_db
DATABASE_URL=postgresql://username:password@localhost:5432/audionoise_db

# =============================================================================
# SESSION & AUTHENTICATION
# =============================================================================

# [REQUIRED*] Session secret for cookie signing
# SECURITY: Use a cryptographically secure random string (min 32 characters)
# Generate with: openssl rand -base64 32
# In development: A random secret is auto-generated if not provided
# In production: This is REQUIRED - server will fail to start without it
SESSION_SECRET=your-cryptographically-secure-random-string-min-32-chars

# =============================================================================
# ENCRYPTION
# =============================================================================

# [REQUIRED*] Master key for AES-256-GCM encryption of sensitive data
# SECURITY: Must be exactly 64 hex characters (32 bytes)
# Generate with: openssl rand -hex 32
# Used for: Encrypting API keys and other sensitive stored data
# In development: A deterministic key is used (insecure, for testing only)
# In production: This is REQUIRED - encryption operations will fail without it
ENCRYPTION_MASTER_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# =============================================================================
# CORS (Cross-Origin Resource Sharing)
# =============================================================================

# [OPTIONAL] Comma-separated list of allowed origins for CORS
# Leave empty to allow all origins in development
# In production, specify your domain(s)
# Example: https://audionoise.app,https://www.audionoise.app
ALLOWED_ORIGINS=

# =============================================================================
# APPLICATION URL
# =============================================================================

# [OPTIONAL] Public URL of your application
# Used for: Email links, OAuth callbacks, Stripe redirects
# Default: http://localhost:5000 (or uses REPLIT_DEV_DOMAIN in Replit)
APP_URL=http://localhost:5000

# =============================================================================
# EMAIL / SMTP CONFIGURATION
# =============================================================================
# Required for: Email verification, password reset, support tickets

# [OPTIONAL] SMTP server hostname
# Examples: smtp.gmail.com, smtp.sendgrid.net, email-smtp.us-east-1.amazonaws.com
SMTP_HOST=smtp.example.com

# [OPTIONAL] SMTP server port
# Common values: 587 (TLS), 465 (SSL), 25 (insecure, not recommended)
# Default: 587
SMTP_PORT=587

# [OPTIONAL] Enable secure connection (true for port 465)
# Default: false (uses STARTTLS on port 587)
SMTP_SECURE=false

# [OPTIONAL] SMTP authentication username
# For Gmail: your full email address
# For SendGrid: apikey
SMTP_USER=your-smtp-username

# [OPTIONAL] SMTP authentication password
# SECURITY: Use app-specific passwords for Gmail (not your account password)
# For SendGrid: Your API key
SMTP_PASS=your-smtp-password

# [OPTIONAL] "From" email address for outgoing emails
# Must be a valid email address
# Default: noreply@audionoise.app
SMTP_FROM=noreply@example.com

# [OPTIONAL] Alternative "From" email (used by email module)
# Default: noreply@audionoise.app
FROM_EMAIL=noreply@example.com

# =============================================================================
# DEFAULT ADMIN USER
# =============================================================================
# Creates an admin user on first startup if these are set
# TIP: Remove or clear these values after initial setup in production

# [OPTIONAL] Admin user email address
# Must be a valid email format
DEFAULT_ADMIN_EMAIL=

# [OPTIONAL] Admin user password
# SECURITY: Use a strong password (minimum 8 characters, 12+ recommended)
# Tip: Include uppercase, lowercase, numbers, and special characters
DEFAULT_ADMIN_PASSWORD=

# [OPTIONAL] Admin user first name
# Default: Admin
DEFAULT_ADMIN_FIRST_NAME=Admin

# [OPTIONAL] Admin user last name
# Default: User
DEFAULT_ADMIN_LAST_NAME=User

# =============================================================================
# STRIPE PAYMENT PROCESSING
# =============================================================================
# Required for: Subscription billing, payment processing
# Get keys from: https://dashboard.stripe.com/apikeys

# [OPTIONAL] Stripe secret API key
# Format: sk_test_... (test mode) or sk_live_... (production)
# SECURITY: Never use live keys in development/test environments
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# [OPTIONAL] Stripe webhook signing secret
# Get from: https://dashboard.stripe.com/webhooks (click on your endpoint)
# Format: whsec_...
# Required for: Processing subscription events, payment confirmations
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret

# =============================================================================
# AI INTEGRATIONS
# =============================================================================
# Used for: AI-powered audio effects, chat, image, and audio generation

# [OPTIONAL] OpenAI API key (or compatible provider)
# Get from: https://platform.openai.com/api-keys
AI_INTEGRATIONS_OPENAI_API_KEY=sk-your-openai-api-key

# [OPTIONAL] Custom base URL for OpenAI-compatible API
# Leave empty to use default OpenAI endpoint
# For Replit AI: This is typically auto-configured
AI_INTEGRATIONS_OPENAI_BASE_URL=

# =============================================================================
# ANALYTICS & ADMIN
# =============================================================================

# [OPTIONAL] Comma-separated list of user IDs with admin analytics access
# These users can view system-wide analytics dashboards
ADMIN_USER_IDS=

# =============================================================================
# AUDIO PROCESSING
# =============================================================================

# [OPTIONAL] Enable JACK audio connection bridge
# Only enable if you have JACK audio server installed and configured
# Default: false
JACK_BRIDGE_ENABLED=false

# =============================================================================
# DEVELOPMENT / DEBUGGING
# =============================================================================

# [OPTIONAL] Enable debug logging
# DEBUG=false

# [OPTIONAL] Log level: error | warn | info | debug
# LOG_LEVEL=info


# =============================================================================
# SECURITY CHECKLIST FOR PRODUCTION
# =============================================================================
#
# Before deploying to production, ensure:
#
# [ ] NODE_ENV is set to "production"
# [ ] SESSION_SECRET is a unique, cryptographically secure string (32+ chars)
# [ ] ENCRYPTION_MASTER_KEY is a unique 64-character hex string
# [ ] ALLOWED_ORIGINS contains only your production domain(s)
# [ ] DATABASE_URL points to your production database (with SSL if remote)
# [ ] SMTP credentials are for a production email service
# [ ] STRIPE_SECRET_KEY is your live key (sk_live_...) NOT test key
# [ ] DEFAULT_ADMIN_PASSWORD is strong and unique (or credentials removed)
# [ ] All API keys are production keys, not test/development keys
# [ ] This .env file is NOT committed to version control
# [ ] File permissions are restricted (chmod 600 .env)
#
# Generate secure values:
#   Session secret:   openssl rand -base64 32
#   Encryption key:   openssl rand -hex 32
#   Secure password:  openssl rand -base64 24
#
# =============================================================================

# Donation wallet address (displayed on support pages)
VITE_DONATION_ETH_ADDRESS=0xYourEthAddressHere
