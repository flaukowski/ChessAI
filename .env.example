# =============================================================================
# AudioNoise Web - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# NEVER commit your actual .env file to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------

# Node environment: development, production, or test
NODE_ENV=development

# Port to run the server on (default: 5000)
PORT=5000

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------

# PostgreSQL connection URL
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/audionoise

# -----------------------------------------------------------------------------
# Session Security
# -----------------------------------------------------------------------------

# Session secret for signing cookies (min 32 characters)
# Generate with: openssl rand -base64 32
# REQUIRED in production!
SESSION_SECRET=your-super-secret-session-key-min-32-chars

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------

# Allowed origins for CORS (comma-separated)
# Leave empty in development to allow all origins
# Example: https://app.example.com,https://www.example.com
ALLOWED_ORIGINS=

# -----------------------------------------------------------------------------
# Email Configuration (SMTP)
# -----------------------------------------------------------------------------

# SMTP server host
SMTP_HOST=smtp.example.com

# SMTP server port (465 for SSL, 587 for TLS)
SMTP_PORT=587

# SMTP username
SMTP_USER=your-smtp-username

# SMTP password
SMTP_PASS=your-smtp-password

# From email address for outgoing emails
SMTP_FROM=noreply@example.com

# Application URL (used in email links)
APP_URL=http://localhost:5000

# -----------------------------------------------------------------------------
# Default Admin User (Optional)
# -----------------------------------------------------------------------------
# If set, a default admin user will be created on first startup.
# Remove or leave empty in production after initial setup.

# Admin email address
DEFAULT_ADMIN_EMAIL=

# Admin password (min 8 characters, recommend 12+)
DEFAULT_ADMIN_PASSWORD=

# Admin first name (optional)
DEFAULT_ADMIN_FIRST_NAME=Admin

# Admin last name (optional)
DEFAULT_ADMIN_LAST_NAME=User

# -----------------------------------------------------------------------------
# AI Integration (Optional - BYOK)
# -----------------------------------------------------------------------------
# Users can configure their own API keys in the app settings.
# These are stored encrypted in the database per-user.

# Default OpenAI API key (optional, for AI effect suggestions)
# OPENAI_API_KEY=sk-...

# Default Anthropic API key (optional)
# ANTHROPIC_API_KEY=sk-ant-...

# -----------------------------------------------------------------------------
# Advanced Configuration
# -----------------------------------------------------------------------------

# Enable debug logging (true/false)
# DEBUG=false

# Log level: error, warn, info, debug
# LOG_LEVEL=info
